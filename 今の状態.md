# 今の状態(1年後に見返す用)

最終更新: 2026年8月17日

このアプリは完成済みです。App Storeのアプリではなく、自分専用のウェブアプリです。

合言葉・GASのURL・Geminiのキー・シートIDは、このファイルにもGitHubにも書いていません。
値そのものは Apps Script のスクリプトプロパティ、または iPhone のアプリ設定画面にあります。

## 何がどこにあるか

画面・裏側・データの3つに分かれています。どれか1つが消えても、残りから復旧できます。

### 1. 画面(スマホで開く部分)

- 公開URL: https://yoshi10659.github.io/health-log-app/
- コード置き場: https://github.com/yoshi10659/health-log-app (GitHubアカウント `yoshi10659`、公開リポジトリ)
- PC上のコピー: `C:\Users\81905\src\health-log-app`
- GitHub Pages は `main` ブランチの `docs/` フォルダを公開している

### 2. 裏側(シートへの書き込みとAI判定)

- スプレッドシートに紐づいた Google Apps Script のウェブアプリ
- 開き方: シートのメニュー「拡張機能」→「Apps Script」
- Geminiのキー・合言葉(`TOKEN`)・シートID(`SPREADSHEET_ID`)は、Apps Script の「プロジェクトの設定」→「スクリプトプロパティ」にある
- コードを直したあとは「デプロイ」→「デプロイを管理」→鉛筆→「新バージョン」→「デプロイ」

### 3. データ本体

- いま使っている Google スプレッドシートが本番
- 食事の1件ずつの正は「食事明細」タブ
- 日次の見た目は「YYYY」(例: 2026) と「YYYY日常記録」
- 定番メニューは「定番マスタ」、目標値は「設定」タブ

## 携帯にどう残っているか

App Store からは入れていません。Safari の「ホーム画面に追加」で作ったショートカットです。

- ホーム画面のアイコン「記録」を押すと、上記の GitHub Pages を全画面で開く
- 設定画面に入れた GAS の URL と合言葉は、iPhone の Safari 保存領域(`localStorage`)に残る。端末の中だけ
- ホーム画面のアイコンを消しても、シートのデータは消えない。URL を Safari で開き直して、もう一度「ホーム画面に追加」すれば戻る
- 機種変更した場合、シートと GitHub はそのまま使える。新しい端末で同じ URL を開き、設定に合言葉を入れ直す

## コミット

2026年8月17日時点で、ブランチは `main`、GitHub と一致、未コミットの変更はなし。

主な履歴(古い順):

1. アプリ一式の初回
2. GAS全部入りファイル追加
3. 文字コード修正
4. Geminiモデル更新(`gemini-3.6-flash`)
5. 食事明細・数量かけ算・修正画面
6. 写真の複数選択

コードを直したくなったら、PC の `C:\Users\81905\src\health-log-app` を開くか、GitHub の同じリポジトリを開く。

## セキュリティ

自分専用・健康記録として普通に十分な水準。銀行や他人向け製品としては足りない。

守れていること:

- Gemini の API キーは GAS の中だけ。GitHub にもスマホの画面コードにも出ていない
- 合言葉が違う通信は全部拒否する
- シートへの書き込みは「あなたの Google アカウント」として動く
- リポジトリは公開だが、中身はプログラムだけ。合言葉・キー・シートIDは入っていない

弱い点:

- GAS は「アクセスできるユーザー: 全員」。URL と合言葉の両方を知った人はシートを読み書きできる
- 合言葉は iPhone 内に平文で保存される(一般的なウェブアプリと同じ)
- 食事写真・音声は判定のため Google(Gemini) に送られる
- GitHub の画面 URL は誰でも開ける。合言葉が無いとシートには届かない

やってはいけないこと:

- 合言葉や GAS の URL を SNS・公開チャットに貼らない
- スプレッドシートを「リンクを知っている全員」で共有しない(自分だけが開ける状態を維持)

合言葉が漏れたと思ったら、Apps Script のスクリプトプロパティ `TOKEN` を新しい文字列に変え、iPhone の設定画面に入れ直せば無効化できる。

## 1年後のよくある操作

| やりたいこと | 場所 |
|---|---|
| アプリを開く | iPhone のホーム画面「記録」、または GitHub Pages の URL |
| コードを見る・直す | この GitHub リポジトリ、または PC の上記フォルダ |
| 裏側を直す | シート → 拡張機能 → Apps Script。直したら新バージョンで再デプロイ |
| 合言葉を忘れた | Apps Script のスクリプトプロパティ `TOKEN` |
| 年が変わる | アプリが `YYYY` / `YYYY日常記録` を自動作成する想定 |
| 食事を直す | アプリの「食事の一覧・修正」。シートの食事セルを直接消さない |

セットアップの手順は [README.md](README.md) を見る。
